# Teacher model configuration
# Uses a standard pre-trained BERT checkpoint from Hugging Face

name_or_path: "bert-base-uncased"
hidden_size: 768
num_hidden_layers: 12
num_attention_heads: 12
intermediate_size: 3072
